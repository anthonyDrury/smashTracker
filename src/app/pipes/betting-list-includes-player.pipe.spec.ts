import { BettingListIncludesPlayerPipe } from './betting-list-includes-player.pipe';

describe('BettingListIncludesPlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new BettingListIncludesPlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
